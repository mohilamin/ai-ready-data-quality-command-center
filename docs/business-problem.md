# Business Problem

AI projects fail when teams cannot trust the data feeding models, dashboards, automations, and agents. Common blockers include duplicate customers, stale source loads, invalid relationships, missing required values, inconsistent dates, unauthorized access, and exposed sensitive fields.

This project simulates those conditions and shows a practical remediation pattern: detect the issues, quarantine bad records, publish clean datasets, document the evidence, and score AI readiness.
